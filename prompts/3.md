# Prompt

Refactor incrementally: improve structure and readability without rewriting from scratch or changing API/input-output contracts.

## Result

I’ll do an incremental refactor of the Go checkout module by extracting small helper functions and clarifying naming while preserving behavior and public API, then run the test suite to confirm no regressions.

I’ve gathered enough context and will now refactor only legacy_checkout.go by extracting discount, shipping, and tax calculations into helpers while keeping the exact rule order and API unchanged.

I’ve gathered enough context and will now refactor only legacy_checkout.go by extracting discount, shipping, and tax calculations into helpers while keeping the exact rule order and API unchanged.
