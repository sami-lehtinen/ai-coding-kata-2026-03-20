# Prompt

I want to improve code readability even further. At least use switch instead of nested if statements and add enum for customer type and country. Throw error in case of unsupported enums.

## Result

- Enums added
- Error is thrown if enum is not valid
- Tests modified accordingly
